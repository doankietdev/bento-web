import { LoginView } from '@/sections/auth/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Login'
};

export default function LoginPage() {
  return <LoginView />;
}
